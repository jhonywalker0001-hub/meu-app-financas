name: Build APK - Minhas Finanças

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Cache Buildozer (acelera builds seguintes)
        uses: actions/cache@v4
        with:
          path: |
            .buildozer
            ~/.buildozer
            ~/.cache/pip
          key: ${{ runner.os }}-buildozer-${{ hashFiles('buildozer.spec') }}
          restore-keys: |
            ${{ runner.os }}-buildozer-

      - name: Instalar dependências do sistema
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y python3 python3-pip openjdk-17-jdk wget unzip git zip

      - name: Instalar Buildozer
        run: |
          pip3 install --user buildozer cython==0.29.36

      - name: Build APK com Buildozer
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          # Corrige permissões (resolve o erro de chown)
          sudo chown -R $USER:$USER .
          # Aceita automaticamente todas as licenças do Android
          yes | buildozer -v android debug

      - name: Upload do APK gerado
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: App-Financas-APK
          path: bin/*.apk
