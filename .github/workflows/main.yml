name: Build APK - Minhas Finanças

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Cache Buildozer (acelera muito as builds)
        uses: actions/cache@v4
        with:
          path: |
            .buildozer
            ~/.buildozer
            ~/.cache/pip
          key: ${{ runner.os }}-buildozer-${{ hashFiles('buildozer.spec') }}
          restore-keys: |
            ${{ runner.os }}-buildozer-

      - name: Instalar dependências do sistema
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-pip openjdk-17-jdk wget unzip

      - name: Instalar Buildozer e dependências
        run: |
          pip3 install --user buildozer cython==0.29.33 virtualenv

      - name: Build APK com Buildozer
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          buildozer -v android debug

      - name: Upload do APK gerado
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: App-Financas-APK
          path: bin/*.apk
